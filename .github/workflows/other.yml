name: Build 4_Other.md from Files

on:
  push:
    paths:
      - Files/4_Other/**
      - scripts/build_other_readme.py

permissions:
  contents: write

concurrency:
  group: repo-write
  cancel-in-progress: false

jobs:
  build-4-other:
    # Only run this job for human-triggered pushes.
    # Bot-triggered pushes (from auto-commits) are ignored to avoid recursion.
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build 4_Other.md
        run: python scripts/build_other_readme.py

      - name: Commit 4_Other.md
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add 4_Other.md
          git commit -m "Auto-update 4_Other.md from Files"
          git push